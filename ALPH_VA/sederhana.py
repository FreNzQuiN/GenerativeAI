from ALPH_VA.core import module_manager
from core import config_manager, speech_to_text, text_to_speech

import os, time, json
from typing import Any, Dict, List, Optional, Tuple

# class SimplePrivateAssistance:
#     def __init__(self):
        

if __name__ == "__main__":
    # assistant = SimplePrivateAssistance()
    text = "Hello, this is a test."
    speaker_id = 1
    # audio_file = assistant.voicevox_api.generate_speech(text, speaker_id)
    # if audio_file:
    #     print(f"Generated audio file: {audio_file}")
    # else:
    #     print("Failed to generate audio.")
        
        